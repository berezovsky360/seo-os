name = "seo-os-router"
main = "worker.js"
compatibility_date = "2024-12-01"

# Route: all *.seo-os.com subdomains
# Add via: npx wrangler deploy
# Then in CF Dashboard → Workers Routes: *.seo-os.com/* → seo-os-router

[[r2_buckets]]
binding = "R2"
bucket_name = "seo-os-sites"  # ← change to your actual bucket name
