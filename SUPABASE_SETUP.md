# Supabase Setup Guide - Phase 1

## ✅ Что уже готово

- ✅ SQL миграции созданы (`supabase/migrations/`)
- ✅ Supabase клиенты созданы (`lib/supabase/`)
- ✅ Middleware для auth создан
- ✅ Тестовый API route создан
- ✅ .env.local обновлен

---

## 📝 Инструкции по настройке

### 1. Создайте проект Supabase (если еще не создали)

1. Перейдите на [supabase.com](https://supabase.com)
2. Войдите или зарегистрируйтесь
3. Нажмите **"New Project"**
4. Заполните форму:
   - **Name:** `seo-os` (или любое имя)
   - **Database Password:** создайте надежный пароль (**сохраните его!**)
   - **Region:** выберите ближайший к вам регион
   - **Pricing Plan:** Free tier
5. Нажмите **"Create new project"**
6. Дождитесь создания проекта (~2 минуты)

---

### 2. Получите API ключи

1. После создания проекта, перейдите в **Settings** (⚙️) → **API**
2. Скопируйте следующие значения:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public** ключ (длинная строка начинающаяся с `eyJ...`)
   - **service_role** ключ (**секретный!** не показывайте никому)

---

### 3. Обновите .env.local

Откройте `.env.local` и замените placeholder значения:

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Перезапустите dev server после изменения .env.local:**

```bash
# Остановите текущий сервер (Ctrl+C)
npm run dev
```

---

### 4. Выполните SQL миграции

1. В Supabase Dashboard перейдите в **SQL Editor** (слева в меню)
2. Нажмите **"New query"**

#### Миграция 1: Создание таблиц

Скопируйте и выполните содержимое файла:
`supabase/migrations/20260207000000_initial_schema.sql`

1. Откройте файл в редакторе
2. Скопируйте весь SQL код
3. Вставьте в SQL Editor в Supabase
4. Нажмите **"Run"** (▶️)
5. Убедитесь что появилось сообщение "Success. No rows returned"

#### Миграция 2: RLS политики

Скопируйте и выполните содержимое файла:
`supabase/migrations/20260207000001_rls_policies.sql`

1. Создайте новый query в SQL Editor
2. Скопируйте весь SQL код из файла RLS policies
3. Вставьте в SQL Editor
4. Нажмите **"Run"** (▶️)
5. Убедитесь что нет ошибок

---

### 5. Проверьте таблицы

1. Перейдите в **Table Editor** (слева в меню)
2. Вы должны увидеть 5 таблиц:
   - ✅ sites
   - ✅ posts
   - ✅ keywords
   - ✅ daily_stats
   - ✅ generated_articles

---

### 6. Создайте тестового пользователя

1. Перейдите в **Authentication** → **Users**
2. Нажмите **"Add user"** → **"Create new user"**
3. Заполните:
   - **Email:** ваш email (например: `test@example.com`)
   - **Password:** создайте пароль
   - **Auto Confirm User:** включите (чтобы не подтверждать email)
4. Нажмите **"Create user"**

---

### 7. Проверьте подключение

Откройте в браузере:
```
http://localhost:3000/api/test
```

Вы должны увидеть JSON ответ:
```json
{
  "success": true,
  "message": "Supabase connection successful!",
  "database": "Connected",
  "tablesAccessible": true,
  "authenticated": false,
  "userEmail": "Not authenticated",
  "sitesCount": 0
}
```

Если видите ошибку - проверьте:
- ✅ .env.local правильно заполнен
- ✅ Dev server перезапущен после изменения .env
- ✅ SQL миграции выполнены успешно

---

## 🎯 Следующие шаги (Phase 2)

После успешной настройки Supabase:

1. ✅ Создать service layer (siteService.ts, keywordService.ts)
2. ✅ Создать React Query hooks
3. ✅ Добавить Auth Context
4. ✅ Заменить mock данные на реальные

---

## 🔒 Безопасность

⚠️ **ВАЖНО:**
- Никогда не коммитьте `.env.local` в git
- `service_role` ключ дает полный доступ к БД - храните в секрете
- В production используйте шифрование для WordPress паролей
- RLS политики защищают данные пользователей друг от друга

---

## 📚 Полезные ссылки

- [Supabase Documentation](https://supabase.com/docs)
- [Supabase Auth with Next.js](https://supabase.com/docs/guides/auth/server-side/nextjs)
- [Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)

---

## ❓ Troubleshooting

### Ошибка: "relation 'sites' does not exist"
→ SQL миграции не выполнены. Выполните миграции в SQL Editor.

### Ошибка: "Invalid API key"
→ Проверьте что ключи скопированы правильно в .env.local

### Ошибка: "Failed to fetch"
→ Проверьте NEXT_PUBLIC_SUPABASE_URL правильный

### Сервер не видит новые env переменные
→ Перезапустите dev server полностью (Ctrl+C → npm run dev)
